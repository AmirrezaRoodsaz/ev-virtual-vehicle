[pytest]
testpaths = tests
# matplotlib drags in deprecated pyparsing calls; not ours to fix and it
# buries real warnings in CI output.
filterwarnings =
    ignore::DeprecationWarning:matplotlib.*
    ignore:.*(resetCache|enablePackrat).*:DeprecationWarning
